# GovCloud (US) deployment overlay
Pins `region = us-gov-west-1` and `gateway_mode = portable`. Validate Bedrock model
and AgentCore feature availability per `../../docs/COMPLIANCE-CONTROL-MAPPINGS.md` and
repo-root `SOURCES.md` before applying.
